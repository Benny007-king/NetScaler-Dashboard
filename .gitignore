# Environment / secrets
.env
.env.local

# Runtime config & state (contain credentials / live data — never commit)
nodes_config.json
auth_config.json
ldap_config.json
ha_last_state.json
failover_history.json

# Flask session secret
.app_secret

# TLS material
certs/
*.pem
*.key
*.crt

# Logs
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
