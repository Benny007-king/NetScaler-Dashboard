# Environment / secrets
.env
.env.local

# Persistent runtime data (all config, DB, certs, secret live here)
data/

# Runtime config & state (contain credentials / live data — never commit)
nodes_config.json
auth_config.json
ldap_config.json
ha_last_state.json
failover_history.json

# Local history database (sessions + failover), with WAL sidecars
dashboard.db
dashboard.db-wal
dashboard.db-shm

# Flask session secret
.app_secret

# TLS material
certs/
*.pem
*.key
*.crt

# Logs
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
